# ── API ───────────────────────────────────────────────────────────────────────
# Points the frontend to the NestJS API.
# For local development, the API runs on acco.localhost:3001.
# For a shared/deployed backend, replace with the hosted URL.
NEXT_PUBLIC_API_URL=http://acco.localhost:3001/api/v1
